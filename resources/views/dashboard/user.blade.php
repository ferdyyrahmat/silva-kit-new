@extends('layouts.vertical', ['title' => 'My Dashboard'])

@section('content')
<div class="container-fluid"><div class="py-3 d-flex justify-content-between align-items-center"><div><h4 class="fs-18 fw-semibold mb-1">Hello, {{ $user->name }}</h4><p class="text-muted mb-0">Track your support requests and stay up to date.</p></div><a href="{{ route('v1.tickets.index') }}" class="btn btn-primary"><i class="mdi mdi-plus me-1"></i>New ticket</a></div>
<div class="row g-3 mb-4">@foreach ([['total','Total tickets','mdi-ticket-outline','primary'],['active','Active requests','mdi-clock-outline','warning'],['resolved','Resolved','mdi-check-circle-outline','success'],['unread','Unread notifications','mdi-bell-outline','info']] as $card)<div class="col-sm-6 col-xl-3"><div class="card border-0 shadow-sm"><div class="card-body d-flex justify-content-between"><div><span class="text-muted fs-12">{{ $card[1] }}</span><h3 class="mt-2 mb-0">{{ $stats[$card[0]] }}</h3></div><div class="avatar-sm rounded-circle bg-{{ $card[3] }}-subtle text-{{ $card[3] }} d-flex align-items-center justify-content-center"><i class="mdi {{ $card[2] }} fs-22"></i></div></div></div></div>@endforeach</div>
<div class="card border-0 shadow-sm"><div class="card-header bg-transparent d-flex justify-content-between"><h5 class="mb-0">My recent tickets</h5><a href="{{ route('v1.tickets.index') }}">View all</a></div><div class="card-body p-0"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><tbody>@forelse($recentTickets as $ticket)<tr><td class="ps-3"><a class="fw-semibold" href="{{ route('v1.tickets.show', $ticket->ticket_code) }}">{{ $ticket->ticket_code }}</a><div class="text-muted fs-12">{{ $ticket->subject }}</div></td><td><span class="badge bg-light text-dark">{{ str_replace('_', ' ', ucfirst($ticket->status)) }}</span></td><td class="text-muted fs-12">{{ $ticket->created_at->diffForHumans() }}</td></tr>@empty<tr><td class="p-5 text-center"><i class="mdi mdi-ticket-outline fs-36 text-muted"></i><p class="text-muted mb-0 mt-2">You have not created a ticket yet.</p></td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
